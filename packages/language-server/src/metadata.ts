/**
 * Canonical identity of this language server. Used as the diagnostic `source`,
 * the completion item source label, and the prefix on user-facing messages.
 */
export const SERVER_NAME = 'drupal-sdc-lsp';
