{
  "name": "drupal-sdc-lsp-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "check:deps": "madge --circular packages/*/src"
  },
  "devDependencies": {
    "madge": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "ts-lsp-client": "^1.0.0",
    "@vitest/coverage-v8": "^2.1.0"
  }
}
